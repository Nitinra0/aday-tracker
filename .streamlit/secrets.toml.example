# Copy this file to .streamlit/secrets.toml for local use, and NEVER commit
# the real secrets.toml to GitHub (it's already in .gitignore).
#
# On Streamlit Community Cloud, don't upload this file at all — instead paste
# its contents into your app's Settings -> Secrets box after deploying.

admin_password = "change-me-before-the-event"
